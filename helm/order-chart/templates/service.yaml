apiVersion: v1
kind: Service
metadata:
  name: order-service
spec:
  type: ClusterIP
  selector:
    app: order-service
  ports:
    - port: {{ .Values.image.port }}
      targetPort: {{ .Values.image.port }}
